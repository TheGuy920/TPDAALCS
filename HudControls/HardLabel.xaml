<UserControl
    x:Class="HudControls.HardLabel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:HudControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="50"
    d:DesignWidth="200"
    Background="Transparent"
    mc:Ignorable="d">
    <Border
        x:Name="Main"
        Background="Black"
        BorderBrush="Black"
        BorderThickness="1"
        CornerRadius="3">
        <Border.Effect>
            <DropShadowEffect
                BlurRadius="6"
                Direction="270"
                ShadowDepth="4" />
        </Border.Effect>
        <Border.OpacityMask>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Border
                        Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Border}}"
                        Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Border}}"
                        Background="Black"
                        BorderBrush="Black"
                        BorderThickness="1"
                        CornerRadius="1"
                        SnapsToDevicePixels="True" />
                </VisualBrush.Visual>
            </VisualBrush>
        </Border.OpacityMask>
        <Grid>
            <Image
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                Source="{Binding BackgroundImage}"
                Stretch="Fill">
                <Image.OpacityMask>
                    <VisualBrush>
                        <VisualBrush.Visual>
                            <Border
                                Width="{Binding ActualWidth, ElementName=Main}"
                                Height="{Binding ActualHeight, ElementName=Main}"
                                Background="Black"
                                BorderBrush="Black"
                                BorderThickness="{Binding BorderThickness, ElementName=Main}"
                                CornerRadius="{Binding CornerRadius, ElementName=Main}" />
                        </VisualBrush.Visual>
                    </VisualBrush>
                </Image.OpacityMask>
            </Image>
            <TextBlock
                x:Name="TextBlock"
                Margin="5,2"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                FontFamily="Unispace"
                FontSize="{Binding FontSize}"
                Foreground="Black"
                Text="{Binding LabelText}"
                TextAlignment="Center"
                TextWrapping="WrapWithOverflow">
                <TextBlock.Effect>
                    <DropShadowEffect
                        BlurRadius="1"
                        Direction="270"
                        ShadowDepth="1"
                        Color="White" />
                </TextBlock.Effect>
            </TextBlock>
        </Grid>
    </Border>
</UserControl>
